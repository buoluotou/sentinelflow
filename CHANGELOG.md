# Changelog

All notable changes to SentinelFlow are documented here.
The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/).

## [Unreleased]

Phase 3.2 is shipped and tagged as `v1.2.0`; Phase 3.3 design is pending.

## [1.2.0] - 2026-08-31

Phase 3.1 + Phase 3.2: **External Response Adapters** — controlled response execution extends from the offline Mock executor to three real external systems behind one unified `ResponseExecutor` contract, while the v1.1.x safety model stays frozen end to end:

AI → Recommendation → Human Approval → Explicit Execution → Guard / Policy → External Adapter → Shuffle / Wazuh / TheHive → Execution Audit

No automatic approval. No automatic retry. No internal adapter fan-out (Single-Active-Adapter: `EXECUTION_ADAPTER` names exactly ONE adapter). No hidden execution. External systems cannot modify SentinelFlow risk or incident facts, and adapter secrets are never persisted in execution audit records.

**Deployment boundary (accuracy note):** all four adapter IMPLEMENTATIONS (mock / shuffle / wazuh / thehive) exist, but the default configuration and the default test suite stay offline — mock remains the credential-free default, every adapter test runs against offline stub transports, and real Shuffle / Wazuh / TheHive connections require explicit `.env` configuration plus the opt-in `external` test mode (`pytest -m external`). This release adds architectural support for external execution, not a default live connection.

### Added

- **Phase 3.1 — Controlled Response Execution (shipped as `657cb87`, first released here)** — append-only `execution_log` (migration 0009: 11 frozen columns, 8 legal decision×direction combinations, three partial unique indexes, FK `approval_id` ON DELETE NO ACTION); derived-state machine with the 8-word vocabulary; Guard with five rejection codes over `EXECUTABLE_ACTIONS`; deterministic zero-outbound `MockExecutor`; synchronous Execute / Compensation service; API `POST /api/v1/executions` + `.../compensate` + read endpoints (201 = execution fact, not success; Bearer `EXECUTION_TOKEN` on write paths only); React Execute Console and Execution Audit UI. Backend 875 passed + 11 real-browser E2E journeys at release.
- **External adapter architecture (Phase 3.2.1)** — shuffle / wazuh / thehive move from RESERVED to RECOGNIZED architecture slots with fail-closed startup validation (`validate_adapter_config` in the lifespan gate); the Single-Active-Adapter invariant rejects every multi-value selection; config errors name setting KEYS only — credential values never surface (repr masking included).
- **Adapter credential boundary (Phase 3.2.2)** — one independent Secret Boundary for every external credential: the ONLY legal life of a secret is `.env → Settings → AdapterCredentials → Authorization header`. URL shape gate (query strings / fragments / userinfo rejected — a credential can never ride inside a URL), repr-masked credentials (Bearer api-key + Basic user/password shapes), `service._append` as the single audit `***` redaction gate, static 503 for adapter misconfiguration, and a `SecretRedactionFilter` for Python logging.
- **Shuffle workflow adapter (Phase 3.2.3)** — workflow orchestration: each executable action triggers EXACTLY ONE pre-configured workflow (frozen flat `SHUFFLE_WORKFLOW_*` mapping, fail-closed on any empty id); OPTIONAL reverse workflows gate compensation capability; E4 semantics `succeeded = trigger confirmed`, zero auto-retry; external execution id tracking (`external_execution_id`) for idempotency propagation. conftest gains the `external` marker: real-instance tests are DESELECTED unless the run opts in with `-m external`, so the default suite stays zero-outbound.
- **Wazuh endpoint response adapter (Phase 3.2.4)** — endpoint / security response: the frozen vocabulary `isolate_host` / `disable_account` / `block_source_ip` via Wazuh `POST /agents/{id}/active-response`, authenticated with the `WAZUH_API_USER` / `WAZUH_API_PASSWORD` pair (Basic) through the 3.2.2 boundary. Compensation is symmetric where the endpoint allows (isolate → release, block → unblock); `disable_account` refuses compensation outright — account state is a human decision.
- **TheHive case adapter (Phase 3.2.5)** — case creation, not a response engine: `escalate_to_incident` posts the frozen six-field body to `POST /api/case` with `sentinelflow_execution_id` for idempotency / audit / external tracking, then human investigators take over (never auto-investigation, never auto-close). Success requires HTTP 200/201 AND a `case_id` (case identity, not a success flag); 409 duplicates resolve to `succeeded + idempotent_duplicate` unless the response claims another execution's id or contradicts the event (fail closed); zero compensation — the case lifecycle belongs to the investigation.
- **E1 capability policy** — `EXECUTABLE_ACTIONS` grows 3 → 4 (`escalate_to_incident`, the ONLY Phase 3.2 capability expansion) while `NON_COMPENSATABLE_ACTIONS` freezes it as non-compensable: escalating to a case has no machine reversal. `hunt_related_activity` / `monitor_only` stay advisory, not machine-executable.
- **Fail-closed outcome handling and protocol-violation validation** — frozen outcome matrix per adapter (timeouts / HTTP faults / unavailable providers classified in `detail`); D9-shaped ambiguous adapter answers (success without identity) raise `ExecutorOutcomeViolation` and only the platform's D9 parser judges `protocol_violation`; zero automatic retry anywhere.

### Validation

- Backend: **1132 passed, 0 failed, 0 skipped** (875 → 911 → 950 → 991 → 1048 → 1132 across Steps 3.1 → 3.2.5)
- External adapter tests are opt-in (`pytest -m external`): 3 deselected by default
- Default test suite performs **zero** real external requests
- `v1.1.0` (tag on `0f6e3fc`) and `v1.0.0-phase1` untouched; Phase 3.2 shipped as five atomic commits `59e78d1` → `2be74f8`, each independently rebuilt and regression-verified

## [1.1.0] - 2026-08-27

Phase 2: AI-assisted analysis behind a human approval queue — AI advises, humans decide, execution stays out of scope (**Approve ≠ Execute**).

### Added

- **AI Provider Architecture (Phase 2 Step 9)** — unified `AIProvider` contract with Mock (default, offline-safe), Ollama (`/api/chat`) and OpenAI-compatible providers ("cloud" is a deployment alias); frozen structured-output protocol `{summary, attack_type, why_risky[], confidence}`; typed error taxonomy (config / unavailable / parse); settings-based registry (`AI_PROVIDER`, `AI_MODEL`, `AI_BASE_URL`, `AI_API_KEY`). AI output is advisory only — no execution.
- **AI alert-explanation data layer (Step 10.1–10.3)** — `ai_analyses` history table (migration 0005; indexed, non-unique `alert_group_id`), evidence-bounded AIRequest builder (max 20 alerts) and `AIAnalysisService` (flush-only, typed errors).
- **AI alert-explanation API (Step 10.4–10.6)** — explicit-trigger endpoints `POST/GET /api/v1/events/{id}/ai-analysis` (201 with full analysis / latest read); error contract 404 (unknown event or no analysis), 503 (provider misconfigured/unreachable), 502 (protocol violation, never persisted). Real Ollama integration verified against local `qwen3:4b` (native JSON mode, configurable `AI_TIMEOUT_SECONDS`); tests stay on the mock provider.
- **Event Detail AI panel (Step 10.7)** — "AI Alert Explanation" panel on the event detail page: latest analysis on load (attack type, summary, why-risky list, confidence, provider/model), explicit "Analyze with AI" trigger with disabled analyzing state and slow-model hint, backend error detail surfaced verbatim; re-analysis appends history, never edits. Browser E2E verified against real `qwen3:4b` and the mock provider.
- **AI risk-summary protocol and task-unified providers (Step 11.1)** — new `risk_summary` task alongside `alert_explanation`; frozen `RiskSummary` protocol `{summary, key_findings[1..5], risk_drivers[vocabulary], analyst_priority(low/medium/high/critical), confidence 0..1}` with a 10-term `RISK_DRIVERS` vocabulary and strict rejection of unknown fields (the AI never emits a new risk score — `EventRisk.score` stays the single official score); `AIProvider.generate(request)` dispatches by task with `explain()` kept as the Step 10-compatible alias; `ai_risk_summaries` history table (migration 0006, indexed non-unique `alert_group_id`) plus optional `prior_explanation` carry-over from Step 10.
- **AI risk-summary request builder (Step 11.2)** — `build_risk_summary_request()` translates AlertGroup + EventRisk + evidence (+ optional latest Step 10 analysis) into a `task=risk_summary` AIRequest; the task is fixed inside the builder (callers cannot steer it), a missing Step 10 analysis never blocks generation, and both tasks share one evidence projection (`MAX_EVIDENCE=20`, earliest first, None-fields dropped); `prior_explanation` is a structured `{summary, attack_type, why_risky, confidence}` projection with no internal ids or provider metadata.
- **AI risk-summary service (Step 11.3)** — `AIRiskSummaryService.generate_risk_summary()` orchestrates Event + EventRisk + evidence (+ optional latest Step 10 analysis) through the task-unified provider and appends a validated `ai_risk_summaries` row; flush-only (the API owns the transaction boundary), history semantics (no overwrite), an `isinstance(RiskSummary)` task guard so a wrong-protocol provider answer surfaces as 502 and never persists, and the Step 10 error contract reused unchanged (404 / 503 / 502, failures never persist). Advisory only — never touches `EventRisk` or `Incident`, never produces execution actions.
- **AI risk-summary API (Step 11.4)** — explicit-trigger endpoints `POST/GET /api/v1/events/{id}/ai-risk-summary` (201 with full summary / latest read ordered `created_at DESC, id DESC`); error contract identical to Step 10 — 404 (unknown event or no summary), 503 (provider misconfigured/unreachable), 502 (protocol violation or wrong-protocol provider answer, never persisted); `AIRiskSummaryRead` mirrors the row field-for-field and never exposes a risk score. Tests inject failing providers through the `get_ai_risk_summary_service` dependency; no real model in CI.
- **AI risk-summary provider/protocol regression and real Ollama E2E (Step 11.5)** — 34 new offline regression tests pinning the frozen protocol: `risk_score` injection rejected via `extra=forbid`, unknown risk drivers rejected with no coercion, `confidence` bounded to [0, 1], `key_findings` bounded to 1..5, `analyst_priority` limited to the frozen enum, and Config / Unavailable / Parse failures never persist a row. Real-model E2E lives under `backend/tests/e2e/` behind a new `ollama` marker (`collect_ignore_glob` keeps the default suite mock-only and offline-safe); verified against live local `qwen3:4b` — full POST→DB→GET chain and dead-URL fault injection (503, zero persisted rows), asserting protocol shape only, never natural-language content.
- **Event Detail AI risk-summary panel (Step 11.6)** — "AI Risk Summary" panel on the event detail page mirroring the Step 10 explanation panel: latest summary on load (GET only — never an automatic POST), 404 rendered as a normal "No risk summary generated yet." empty state, explicit "Generate Risk Summary" trigger with disabled generating state and slow-model hint, 201 response rendered directly without a follow-up GET, and backend 503/502 details surfaced verbatim. Shows analyst priority, confidence, summary, key findings and risk drivers only — never a risk score (EventRisk.score stays the single official score). First frontend test stack: vitest + jsdom + Testing Library, 8 unit tests covering all states plus a smuggled `risk_score` never-render guard.
- **Browser E2E for the AI risk-summary panel (Step 11.7)** — real-browser dual-chain verification against the live console: mock chain proves the full user path (empty state → explicit generate → panel render → refresh loads latest via GET only, never an automatic POST → second generate appends a second history row with the first untouched), and the real `qwen3:4b` chain proves the slow-model path (disabled generating state, ~33s inference under the 180s timeout, protocol-shaped render with frozen driver vocabulary, provider/model surfaced as ollama/qwen3:4b). Both chains leave `EventRisk` and `Incident` untouched and never surface a risk score in the UI.
- **Step 11 final regression and workspace audit (Step 11.8)** — zero-feature acceptance pass closing Step 11: backend 347 passed (0 failed / 0 skipped), frontend 8 passed + zero TypeScript errors + production build; browser E2E mock and real `qwen3:4b` chains re-executed green (risk_score never rendered, EventRisk/Incident unchanged, history append-only); `tests/e2e/` real-model suite re-run explicitly under the `ollama` marker (2 passed; the default suite stays mock-only and offline-safe); migration 0006 verified through `alembic upgrade head` with no `create_all` in the production path; `git diff --check` clean and every changed file traced to Step 11.1–11.8. Released as the single atomic commit `feat(ai): add risk summary`.
- **AI response-recommendation protocol and data model (Step 12.1)** — frozen `response_recommendation` task with a six-action vocabulary `RESPONSE_ACTIONS` (`block_source_ip`, `isolate_host`, `disable_account`, `hunt_related_activity`, `escalate_to_incident`, `monitor_only`; anything else is a 502 protocol violation that never persists); output protocol `{overall_rationale, recommendations[0..5] × {action, target, rationale}, confidence 0..1}` with `extra=forbid` at both layers — an EMPTY recommendations list is a first-class answer ("no action warranted", distinct from "no record yet"); the system prompt forbids execution and risk-score recomputation; optional `prior_summary` carries a five-key projection of the latest Step 11 risk summary; `ai_response_recommendations` history table (migration 0007, indexed non-unique `alert_group_id`); deterministic Mock bands (≥70 block+escalate / 40–69 hunt / <40 empty).
- **AI response-recommendation service and API (Step 12.2–12.3)** — `AIResponseRecommendationService` (flush-only, vocabulary re-check so typed-object providers cannot bypass the frozen word list, failures never persist) behind explicit-trigger endpoints `POST/GET /api/v1/events/{id}/response-recommendation` (201 with the full recommendation / latest read ordered `created_at DESC, id DESC`); the API owns commit + refresh; error contract identical to Steps 10/11 (404 / 503 / 502); `AIResponseRecommendationRead` mirrors the row field-for-field and never exposes a risk score.
- **Response-recommendation cross-layer regression (Step 12.4)** — all six frozen actions round-tripped through the real API path, the three Mock score bands verified end-to-end (empty recommendations treated as success), protocol violations driven through the raw-output Ollama path (unknown action / extra field / risk_score smuggle / confidence out of bounds → 502 with zero new rows), and the POST→GET→POST→GET closed loop proving append-only history with the first record never UPDATEd.
- **Event Detail response-recommendation panel (Step 12.5)** — "Response Recommendation" panel completing the analysis chain (Explanation → Risk Summary → Response Recommendation): latest recommendation on load, 404 empty state strictly distinguished from 200 + `recommendations=[]` ("No response action warranted."), explicit single "Generate Response Recommendation" button, display-layer labels for the six frozen actions (no new protocol enum), and NO execution affordance of any kind — Approve/Execute stays out of the UI until human approval workflows land.
- **Browser E2E for the response-recommendation panel (Step 12.6)** — dual-chain verification: the mock chain proved empty state → generate → refresh (GET only) → second generate (two rows, first untouched, EventRisk/Incident unchanged, all six action labels rendered), and the real `qwen3:4b` chain produced a protocol-shaped recommendation (3 actions, all within the vocabulary, confidence in [0,1]) under the 180s timeout — structure asserted, natural-language content never pinned.
- **Step 12 final regression and atomic commit (Step 12.7–12.8)** — backend 427 passed, frontend 19 passed + zero TypeScript errors + production build; migration 0007 verified via `alembic upgrade head` with no `create_all` in the production path; `git diff --check` clean, all 25 changed files traced to Step 12.1–12.6, no sensitive artifacts. Released as the single atomic commit `feat(ai): add response recommendation`.
- **Approval Queue over AI response recommendations (Phase 2 Step 13)** — human decisions layered on AI advice with Approve ≠ Execute baked into every layer: `ai_response_approvals` table (migration 0008, `UNIQUE(recommendation_id)`, `CHECK status IN ('approved','rejected')` — "pending" is a derived queue state and is never persisted); INSERT-only one-shot decisions with `reviewed_at` server-stamped (clients can only send `reviewer` + optional `review_comment`, `extra="forbid"`); `GET /api/v1/approvals` serves the backend-projected pending queue (`created_at ASC, id ASC`), `POST .../approve|reject` return 201, a repeat decision is 409; React Approval Queue page (read-only recommendations, shared reviewer, double-click guard, 201 local removal vs 409 server re-sync); real-browser Playwright E2E (10 journeys: lifecycle, persistence, refresh, empty queue, concurrent 409, delayed-POST busy state, network whitelist). No AI result or decision ever touches `EventRisk`, `Incident` or any orchestrator — execution remains Phase 3. Backend 505 passed / frontend 35 passed; released as the single atomic commit `feat(ai): add approval queue`.
- **Incident AI association protocol and data model (Step 14.1)** — the incident-centric case view: `Incident` gains read-only (`viewonly`) traversals to its event's `ai_analyses`, `ai_risk_summaries` and `ai_response_recommendations` (approval reachable via `recommendation.approval`), joining on the SAME `alert_group_id` — zero schema change, no new migration, no incident FK on any AI table. Frozen boundaries: `Incident.risk_score` stays the creation-time snapshot of `EventRisk.score` (no AI result writes it back), the AI rows remain the AlertGroup's append-only history (deleting an incident never deletes AI history), and an approved decision is displayed/audited only — never auto-consumed (no Shuffle/Wazuh/TheHive execution).
- **Incident AI context service (Step 14.2)** — `get_incident_ai_context(db, incident_id)`: a pure READ aggregation composing the Step 14.1 viewonly traversals into a frozen `IncidentAIContext` DTO (`IncidentSnapshot{id, status, severity, risk_score_snapshot}` + complete histories `analyses[] / risk_summaries[] / response_recommendations[{recommendation, approval|null}]`, each embedding its existing Step 10–13 protocol schema unchanged, `created_at ASC`). Unknown incident raises the project's unified `IncidentNotFound` before anything is assembled (no cross-case leak); an incident without AI history returns a valid empty context; `approval=None` stays the DERIVED pending state (never persisted). Zero writes: no add/flush/commit, incident snapshot/status/severity and all AI/approval row counts provably unchanged by a read, and an approved recommendation surfaces as audit information only — no execution, no incident transition, no risk recompute.
- **Incident AI context API (Step 14.3)** — `GET /api/v1/incidents/{incident_id}/ai-context`: the incident's complete AI history exposed as a strictly read-only HTTP endpoint. The router is a pure passthrough into the Step 14.2 service — it never queries AI tables itself, never generates/refreshes AI data, never recomputes risk and never touches approvals or the incident state. 404 reuses the unified `"Incident not found"` detail (unknown id AND malformed UUID) with no context body and no cross-case leak; an incident without AI history answers 200 with empty histories; `approval=null` keeps the derived pending semantics (no persisted approval status added). Verified by 7 dedicated API tests (unknown/malformed 404, empty 200, full history + approved/rejected/pending semantics, incident A↔B isolation, HTTP read-only boundary) — backend suite 531 passed.
- **Incident AI integration cross-layer regression (Step 14.4)** — proof that Steps 14.1–14.3 keep the full frozen chain (`AlertGroup -> EventRisk -> Incident -> AI Context` with Explanation/Risk Summary/Recommendation/Approval) intact once combined. New `test_incident_ai_cross_layer_regression.py` drives every AI row through the REAL production endpoints (mock provider: `POST /events/{id}/ai-analysis | ai-risk-summary | response-recommendation`, `POST .../approve|reject`) and observes the result at ORM, service and API layers: full-lifecycle body matches database rows id-for-id; histories stay complete (`created_at ASC`, never latest-overwrites) with approved/rejected/pending coexisting and pending never persisted; `Incident.risk_score` and `EventRisk.score` survive AI activity plus repeated context reads; isolation holds at all three observation layers; an approved chain read end-to-end fabricates no execution, no new recommendation and no status move; empty and analysis-only pipelines are legal context states. Zero production-code changes, zero bugs found — 7 new tests, backend suite 538 passed.
- **React Incident AI view (Step 14.5)** — the Incident Detail page gains a read-only "AI Investigation" panel consuming ONLY `GET /incidents/{id}/ai-context` (one new API-client method `getIncidentAIContext`, GET-only; no generate/approve/reject/execute client added). One protocol end-to-end: new `IncidentAIContext` / `IncidentSnapshot` / `RecommendationWithApproval` TS types compose the EXISTING frozen Step 10–13 types unchanged (plus `AIResponseApproval` mirroring the backend Read schema) — no second frontend protocol. Complete histories render newest-first with nothing dropped (`AI Explanation History (n)` / `Risk Summary History (n)` / `Response Recommendation History (n)`); approvals audit as Approved/Rejected chips with reviewer + reviewed-at + comment, `approval=null` renders as the derived "Pending Review" label (never sent back to the backend); empty and partial pipelines are legal states ("No AI analysis available yet.", never "Failed to load"); only `risk_score_snapshot` is shown. Approve/Reject stay owned by the Approval Queue: the panel renders ZERO buttons. Zero backend changes. Frontend 43 passed (8 new: full/multi-history/approval-states/empty/partial/safety-boundary/snapshot + 404 banner), tsc clean, Vite build OK.
- **Browser E2E for the Incident AI view (Step 14.6)** — real Chromium + real uvicorn + real vite over a throwaway SQLite database, in `backend/tests/e2e/test_incident_ai_context_browser.py` (9 journeys). Every AI row is produced through the REAL production endpoints (mock provider: `POST /events/{id}/ai-analysis | ai-risk-summary | response-recommendation`, `POST .../approve|reject`) — only the event skeleton is seeded, keeping the 14.4 standard. Verified in a genuine browser: full AI context (incident info + risk snapshot + Explanation/Risk Summary/Recommendation/Approval all visible); the three approval states with `approval=null` rendering ONLY "Pending Review" while the database stores exactly `{approved, rejected}` (no pending row); 3×3×3 histories all visible (never latest-only); empty ("No AI analysis available yet.", no error banner) and partial (explanation-only) pipelines healthy; unknown incident → unified 404 "Incident not found" with zero cross-case leakage; the risk snapshot stays 80 after further AI history lands (14.4's freeze, echoed in the browser). Safety boundary lifted to the browser layer: the AI Investigation panel renders ZERO buttons and none of Execute/Block Now/Isolate Now/Disable Now/Run Response/Retry Execution/Approve/Reject; a page load emits ONLY GET .../ai-context (dev-mode StrictMode may repeat the read-only GET once) and zero POSTs of any kind — Observe/Review/Audit, never Decide/Execute. Zero production-code changes (two test-infrastructure fixes: pipe-drain threads so the ~4 KB Windows pipe buffer can no longer deadlock uvicorn mid-run, and widened post-navigation expect timeouts against vite cold compiles). Backend 538 passed; frontend 43 passed / tsc clean / build OK; E2E 9/9 green twice in a row.
- **Step 14 final regression and workspace audit (Step 14.7)** — zero-feature acceptance pass closing Step 14: backend 538 passed (0 failed / 0 skipped), frontend 43 passed + zero TypeScript errors + production build, the Step 14.6 Browser E2E re-executed green (9/9, third consecutive run); migration round-trip verified on a throwaway SQLite (`alembic upgrade head` 0001→0008, `current` = 0008, `downgrade base` full rollback, re-upgrade clean — Step 14 adds ZERO new migrations by design); full 14.1–14.6 change audit: `git diff --check` clean, all 8 tracked production edits additive and step-attributed, the 10 new files all Incident-AI test/schema/service/component artefacts, zero stray .env/DB/tmp/log/screenshot artifacts, and no Phase 3 execution code anywhere (Shuffle/Wazuh/TheHive appear only in negation comments). Security boundaries re-frozen: `EventRisk.score` and `Incident.risk_score` never written by 14.x (snapshot is read-only projected), the context service is provably write-free (no add/flush/commit), approvals stay `{approved, rejected}` with pending derived-only, and the browser layer emits zero mutating traffic. Working tree safe to enter the 14.8 atomic commit.

## [1.0.0-phase1] - 2026-08-25

Phase 1: the complete detection-to-incident SOC platform.

### Added

- **Alert Ingestion** — `POST /api/v1/alerts` + listing/detail; every alert and raw payload preserved as evidence
- **Normalization** — adapter-based unified event model (`POST /api/v1/normalize`); Simulator adapter implemented, Wazuh adapter reserved (501)
- **Deduplication & Aggregation** — SHA-256 fingerprint + 5-minute window (`DEDUP_WINDOW_SECONDS`); fingerprint ≠ group semantics; window expiry opens new events
- **Explainable Risk Engine** — severity / frequency / public-source factors, 0–100 score, four levels; factor breakdown stored per event; recalculated transactionally on the write path only; surfaced in `GET /api/v1/events` (`?level=` filter)
- **Incident Management** — data model, service-layer lifecycle state machine (`open → in_progress → resolved/false_positive → closed`), REST API with 404/409 error contract, automatic creation at risk ≥ 70 (idempotent, snapshot-at-first-threshold-crossing)
- **Dashboard API** — `GET /api/v1/dashboard/summary` real-time aggregation (active incidents, severity breakdown, today's alerts/events, risk distribution)
- **React Web Console** — dark SOC theme; Dashboard (auto-refresh), Events (filter/pagination/detail with risk factors and evidence), Incident Queue (status filter, lifecycle transitions)
- **Scenario Simulator** — 5 attack scenarios + stdlib runner CLI (`--repeat`, `--timestamps now|file`)
- **Docs** — architecture, API reference, demo guide, deployment + security hardening checklist
- **Release engineering** — 202-test backend suite, Alembic migrations 0001–0004, `.env.example` with placeholders only

### Known Limitations (Phase 1)

- No authentication — deploy behind a trusted reverse proxy (see `docs/deployment.md`)
- SQLite is supported for evaluation/CI; PostgreSQL 16 is the production target
