# Scenario Runner

The Scenario Runner will be implemented in Phase 1 Step 5.

It will:
- Load scenario JSON files from the `scenarios/` directory
- Send simulated security events to the SentinelFlow backend API
- Support configurable intervals and repetition
- Provide real-time feedback on sent events
