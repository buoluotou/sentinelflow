-r base.txt
pytest>=8.3.0
httpx>=0.27.0
pytest-cov>=6.0.0
# The browser tests (backend/tests/e2e, marker: browser) need a browser binary,
# which pip does not install. Run once per machine:
#   python -m playwright install chromium
pytest-playwright>=0.9.0
